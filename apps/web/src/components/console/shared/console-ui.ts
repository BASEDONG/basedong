/** Primary CTA — matches API 密钥 create button (Console page grammar). */
export const CONSOLE_PRIMARY_BTN =
  "bd-gradient-bg inline-flex h-10 cursor-pointer items-center justify-center rounded-[12px] border border-transparent px-[15px] text-base font-normal leading-6 text-white transition-opacity hover:opacity-90 disabled:cursor-not-allowed disabled:opacity-50";

/** Compact primary for denser toolbars (filters, inline actions). */
export const CONSOLE_PRIMARY_BTN_COMPACT =
  "bd-gradient-bg inline-flex h-9 cursor-pointer items-center justify-center rounded-[12px] border border-transparent px-4 text-sm font-normal text-white transition-opacity hover:opacity-90 disabled:cursor-not-allowed disabled:opacity-50";

/** Standard card / table shell. */
export const CONSOLE_SURFACE =
  "rounded-[8px] border border-slate-200 bg-white";

/** Table header row background. */
export const CONSOLE_THEAD = "bg-[rgb(248,250,252)]";
