# Control-plane + Relay origin for basedong-api (no trailing slash).
# OpenAI-compatible calls: POST $NEXT_PUBLIC_API_BASE/v1/chat/completions
# with Authorization: Bearer <API Key>
# Local: docker compose -f docker-compose.dev.yml up -d --build in basedong-api
NEXT_PUBLIC_API_BASE=http://localhost:3000
