# Retry overlay — deterministic PICK_ORDER on mock Zen (#13)
# Requires docker-compose.zen-mock.yml

services:
  zen-sidecar:
    environment:
      - PICK_ORDER=fail-free,ok-free
      # Keep mock probe fast: fail-free returns 429, then rotate to ok-free.
      - PER_MODEL_ATTEMPTS=2
